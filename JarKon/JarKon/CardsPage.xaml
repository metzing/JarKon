<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:JarKon"
             x:Class="JarKon.CardsPage">
    <ScrollView>
        <ListView ItemsSource="{x:Static local:MainPage.Vehicles}"
                  HasUnevenRows="True">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <local:Card ID="{Binding ID}"
                                Position="{Binding Position}"
                                Speed="{Binding Speed}"
                                Ignition="{Binding Ignition}"
                                InternalVoltage="{Binding InternalBatteryVoltage}"
                                ExternalVoltage="{Binding ExternalBatteryVoltage}"
                                DriverName="{Binding DriverName}"/>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </ScrollView>
</ContentPage>